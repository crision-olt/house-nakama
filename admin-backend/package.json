{
  "name": "@monorepo/admin-backend",

  "scripts": {
    "dev": "deno run --watch main.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@monorepo/shared": "workspace:*"
  }
}
